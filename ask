#!/bin/bash
set -e
if [ -f .env ]; then
    export $(grep -v '^#' .env | grep -v '^\s*$' | xargs)
fi
if [ -z "$OPENAI_API_KEY" ]; then
    echo "Error: OPENAI_API_KEY is not set. Copy .env.example to .env and add your key."
    exit 1
fi
docker compose run --rm app claire_directllm.ask "$@"
